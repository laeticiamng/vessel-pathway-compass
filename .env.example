# Supabase project credentials (anon-tier client config; safe to expose).
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Feature flags
# Public USD pricing surface. Off during the doctoral / pre-MDR phase;
# Stripe / checkout code remains in the bundle for post-MDR re-enablement.
VITE_PUBLIC_PRICING_ENABLED="false"
